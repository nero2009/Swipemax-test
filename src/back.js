import React from 'react';

const back = (props) => {
    return (
        <div className={"item-payment Card-Back" + props.flipped }style={{justifyContent:"center"}}>
            
        </div>
    );
};

export default back;